;; File for additional machine-local configurations

;; Uncoment to load configs that I want on most machines, but not on
;; minimalist installs:

;;(require 'my-config)

;; Alternatively, manually load parts of my config individually:

;;(require 'my-python)
;;(require 'my-org)
